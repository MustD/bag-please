---
scope: Epic 9 — User Feedback Pass (FR13, FR44, FR56, FR57, FR61, FR66-FR69 + routed carry-ins)
purpose: build-substrate
altitude: epic
updated: 2026-09-15T11:32
---

- (event) Run started (Fast path, build-substrate only, epic altitude). Before drafting, md asked to remove stale
  architecture files: removed planning-artifacts/architecture-epics-1-2.md (superseded by architecture.md),
  docs/architecture-bp_back.md (pre-Epic-1 scan: single admin, /api/login, unauthenticated subscriptions),
  docs/integration-architecture.md (nginx + Next.js); fixed dead links in docs/index.md. Kept
  docs/architecture-routing.md (current Caddy) and architecture.md (parent: Epic 4 backend invariants + Epic 5 frontend
  reframe)
- (constraint) Inherited from architecture.md + bp_back/bp_front CLAUDE.md + code: per-entity vertical slices
  entity/<name>/{Service,Storage,gql/,mongo/}; list-scoped ops verifyMembership in service (NFR-L2); admin blocked from
  list ops by adminLogin compare; admin GQL ops gated by requireAdmin () role claim; startup migrations recorded in
  app_migrations; subscriptions filtered per list with takeWhile membership recheck; single Apollo client; in-memory
  auth token; /api never cached by SW; UI-driven E2E (NFR18); multi-collection cascades are sequential Mongo deletes
  without a transaction (ListService.deleteList convention)
- (decision) Feedback is a new slice entity/feedback/ following the user/config pattern (Service -> Repository, no
  Storage cache, no SharedFlow/subscription); collection 'feedback'; fields id UUID, text, username (denormalized
  string, no userId FK so it survives user deletion), createdAt Instant. Prevents: a story adding a cache/subscription
  or an FK that breaks FR67 survival
- (decision) Feedback authorization: sendFeedback rejects the admin (service-layer adminLogin compare, like
  ListService); feedback list + deleteFeedback use requireAdmin (). Server sets username from principal and createdAt
  from clock; client sends only text; server trims, rejects blank or >2000 chars
- (decision) Stores: one field stores: List<String> in every layer (Item, MongoItem default emptyList, GqlItem
  stores: [String!]!, ItemInput stores: [String!]!); scalar store removed from schema in the same release, no dual
  field. Server is the normalization authority: trim, drop blanks, dedupe case-insensitively (lowercase Locale.ROOT)
  keeping first occurrence casing and input order. Any store comparison anywhere is trim+case-insensitive
- (decision) itemStoreSuggestions returns server-normalized distinct stores (case-insensitive dedupe, sorted
  case-insensitively); picking a suggestion uses its casing
- (decision) FR69 migration id epic9-multi-store in app_migrations. Migration.kt refactored so each migration checks
  only its own record (current epic4 early-return would skip later migrations). Runs in configureMigration before
  configureGql (before any storage sync). Filter-idempotent: only items where stores is absent get
  stores=[trimmed store] or [], then store unset; completion record written last, so a crash mid-run re-runs safely
- (decision) FR13 pagination is server-side offset: users (offset: Int!, limit: Int!) returns UserPage { users,
  totalCount }, sorted by username ascending in Mongo; server clamps limit to 1..100; client derives page from
  totalCount and clamps to last valid page after create/delete
- (decision) Cascades live in services, parent calls child, never child->parent: CategoryService.deleteCategory calls a
  new internal ItemService method that deletes the category's items via ItemStorage and emits an item deletion per item,
  before emitting the category deletion. The frontend client-side delete loop is removed
- (decision) Membership writes (list_members rows and List.members/memberUsernames) are owned by ListService only. User
  deletion: UserAdminMutations.deleteUser calls ListService.purgeUser (user) before UserService.adminDeleteUser, then
  invalidates sessions
- (assumption) Lists OWNED by a deleted user are deleted with full cascade via the deleteList path (an ownerless list
  could never be deleted); non-owner memberships and pending invites are removed. Needs md confirmation: alternative is
  ownership transfer
- (decision) Health: GET /api/health, unauthenticated, outside authenticate {}, not rate-limited; 200 when Mongo ping
  succeeds, 503 otherwise; E2E readiness waits on it through Caddy :2080
- (decision) Frontend: FR68 FAB opens the same AddItemDialog component the management screen uses (no second add
  dialog); StoreField becomes multi-value (string[]); store normalization helper stays in lib/lists/storeValue.ts
  mirroring server rule; account-menu Home and Feedback entries live in AppShell, Feedback hidden for admin role;
  feedback form is a dialog in AppShell (no route) so the user stays on their screen; feedback text rendered as React
  text nodes only
- (assumption) Feedback form is a dialog, not a route
- (decision) Schema-changing stories regenerate codegen in the same story; backend and frontend ship in one release
  version (gradle.properties + package.json), no backward-compatible dual schema
- (version) Reality-checked from repo 2026-09-15: Kotlin 2.4.10, Ktor 3.5.2, graphql-kotlin 10.2.1, MongoDB driver
  5.9.2, mongo:8, React 19.2.8, Apollo Client 4.2.11, MUI 9.3.1, graphql 17.0.2, TypeScript 6.0.3, Vite ^8.2.1,
  Playwright ^1.60.0, app 0.18.0. No new dependency introduced by Epic 9
- (direction) Deferred: Mongo transactions for cascades; feedback pagination; max stores per item; Phase 3 store mode;
  F5 Uncategorized reservation and F2 empty-category visibility (frontend-only, story-level); E2E data-hygiene mechanism
  (story-level, within NFR18); /admin page-in-URL
- (event) Finalize step 2 reconcile (inline, inputs already in context): gaps to apply after the reviewer gate: (a)
  carry-in 'saveItem stamps checkedAt when it checks an item' missing from spine, needs an item-mutation rule next to
  AD-3; (b) AD-5 FR69 conversion must use an aggregation-pipeline update since a plain $set cannot read the store field;
  (c) story-level FR44 carry-ins (EditItemDialog stale comments, orphaned-item dialog closing silently) go in the map
  only. Lint: 0 findings. Gate dispatched: rubric walker, currency lens, adversarial lens
- (event) Reviewer gate results: rubric (0 critical, 3 high, 9 medium, 8 low), currency (4 medium, 2 low, 2 info; all
  Stack rows match repo, backend web-current), adversarial (5 high, 5 medium, 2 low). Full reviews in reviews/
- (decision) AD-5 amended: migration filter is 'store exists' (not 'stores missing'), per-document loop through the
  Kotlin normalizer merging existing stores + store (avoids pipeline-update pitfall and client/Mongo trim drift), ships
  in the AD-3 story, ItemRepository.save unsets store; ordered [epic4-list-seed, epic9-multi-store]; pre-deploy
  mongodump, rollback = restore + old image
- (decision) AD-6 amended: users (limit, offset, around) -> UserPage{users,totalCount,offset}; around returns the page
  containing a username so create lands on the new row without a page-walk (no FR13 change, no search field); binary
  collation; cache-and-network + evict users on mutation; named pagination test ids
- (decision) AD-7 amended: category deleted first, then all items incl. soft-deleted; cascade emits only the category
  DELETED event (SharedFlow is 1-slot DROP_OLDEST); clients prune children on category DELETED on both list pages;
  saveItem create branch now checks category; uncheckItem rejects missing category
- (decision) AD-8 amended: order adminDeleteUser -> purgeUser (userId, username) -> invalidateUserSessions; purge writes
  via ListStorage.save + ListMemberRepository only; private cascadeDeleteList shared with deleteList; list deletion
  emits no event, members redirect on next Forbidden; NFR-L2 inherited row notes the admin-gated purge as the single
  caller-less exception
- (decision) AD-9 amended: get ("/health") under rootPath api; ping in withTimeout (2s), any failure 503; Playwright
  webServer.url http://localhost:2080/api/health; optional compose healthcheck only on bp_front via busybox wget, no
  tool added to bp_back (eclipse-temurin has no curl/wget)
- (decision) AD-2 amended: requireAdmin consolidated into plugins/GqlAuth.kt; trimmed UTF-16 length on both sides, no
  raw maxLength; deleteFeedback returns ID; ConfirmDialog for delete; feedback package registered in GQL.kt; AD-1 _id
  stored as string
- (decision) AD-4 amended: suggestion representative = lowest by (lowercase, compareTo); casing-only edit is a change;
  internal whitespace kept; client trim ()+toLowerCase (), server authoritative
- (decision) New AD-10 detail: shopping row a11y 'Stores: A, B' and store chip test ids; Home reuses useHomePath
  observe + alreadyHome, Lists entry stays. New AD-11: single applyCheckState for check/uncheck/save (resolves checkedAt
  carry-in vs FR58 merge). New conventions: ListItemFields fragment mandatory, notices via existing snackbar, events
  convention
- (question) Open: owned lists of a deleted user deleted vs transferred (blocks user-cascade story); feedback dialog vs
  route (blocks FR66); cold-start home link (md, FR57 scope); F5 Uncategorized reserve vs de-key (blocks FR61)
- (direction) Deferred with revisit conditions: username reuse after deletion (pre-existing, admin-only deletion;
  revisit if self-registration enabled); feedback pagination + sendFeedback rate limit; webServer teardown/stdout
  (story-level); theme tokens cleanup sequenced after AppShell stories
- (event) Gate fixes applied to spine (AD-1..AD-10 amended, AD-11 added, Open Questions section added, conventions for
  events/frontend data/notices); fixed dangling AD-13 refs; re-lint 0 findings. Spine stays draft pending md on 4 open
  questions
- (decision) md 2026-09-15: lists owned by a deleted user are deleted with full cascade (not transferred);
  DeleteUserDialog shows the owned-list count via User.ownedListCount
- (decision) md 2026-09-15: feedback form is a dialog rendered by AppShell, not a route
- (decision) md 2026-09-15: F5 resolved by de-keying the synthetic Uncategorized bucket (sentinel id, frontend-only); no
  reserved category name
- (decision) md 2026-09-15: cold-start home-link question closed as leave-as-is (cache-only observe mode); FR57 story
  only adds the menu entry
- (event) spine finalized
